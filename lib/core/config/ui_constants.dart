import 'package:flutter/material.dart';

// Border radii
const double kCardBorderRadius = 24.0;
const double kButtonBorderRadius = 16.0;

// Opacity
const double kGlassOpacity = 0.55;
const double kPrimaryShadowOpacity = 0.18;

// Animation durations
const Duration kCardEntranceDuration = Duration(milliseconds: 800);
const Duration kContentTransitionDuration = Duration(milliseconds: 400);
const Duration kBoltPulseDuration = Duration(milliseconds: 1200);
const Duration kFavIconDuration = Duration(milliseconds: 300);

// Spacing
const double kDefaultPadding = 24.0;
const double kDefaultSpacing = 16.0;

// Colors (if not from theme)
const Color kFavoriteColor = Colors.amber; 